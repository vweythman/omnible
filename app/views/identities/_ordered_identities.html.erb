<dl class='index'>
<% ordered_identities.each do |key, identities| %>
	<dt><%= key %></dt>
	<% identities.each do |identity| %>
		<dd><%= link_to identity.name, identity %></dd>
	<% end %>
<% end %>
</dl>
