<% provide(:title, @character.title_for_editing) %>

<%= editing_block("Edit") do %>
	<%= render 'form' %>
	<%= render 'subjects/shared/footer' %>
<% end %>
<script>autosize(document.querySelectorAll('textarea'));</script>