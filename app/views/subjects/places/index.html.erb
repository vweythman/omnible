<% provide(:title, "Places") %>

<div id="main" class="index">
	<header>
		<h1>Places</h1>
		<%= creation_toolkit "Place", :place %>
	</header>

	<%= render 'filters' %>
	<%= @places.results %>
</div>

<%= render 'subjects/shared/footer' %>
