<% if @places.length > 0 %>
	<article>
		<dl>
		<% @places.each do |type, group| %>
			<dt><%= type %></dt>
			<% group.each do |place| %>
				<dd><%= link_to place.name, place %></dd>
			<% end %>
		<% end %>
		</dl>
	</article>
<% end %>
