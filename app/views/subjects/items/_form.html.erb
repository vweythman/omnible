<%= form_for(@item) do |f| %>

	<fieldset class="primary">
		<%= string_panel f, :nature, "Describe Type" %>
		<%= string_panel f, :name,   "Name Item"     %>
		<%= taggables(["item","descriptions"], @item.current_tags, "General Tags") %>
	</fieldset>

	<%= submit_panel f %>

<% end %>
