<div class='nested-fields'>
	<fieldset>
		<%= form_field f.label(:quality_id, "Quality"), f.collection_select(:quality_id, Quality.order('lower(name)').all, :id, :name) %>
	</fieldset>

	<p><%= link_to_remove_association "- Remove", f %></p>
</div>