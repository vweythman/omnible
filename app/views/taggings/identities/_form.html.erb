<% remote ||= false %>
<%= clean_form_for @identity, remote: remote do |f| %>

	<fieldset class="primary-fieldset">
		<%= f.string_cell :name,   "Identity Name" %>
		<%= f.string_cell :nature, "Identity Type" %>
	</fieldset>

	<%= f.submit_cell %>

<% end %>