<% remote ||= false %>
<%= form_for @identity, remote: remote do |f| %>

	<fieldset class="primary">
		<%= string_panel f, :nature, "Describe Type" %>
		<%= string_panel f, :name,   "Write Name"    %>
	</fieldset>

	<%= submit_panel f %>

<% end %>