<% remote ||= false %>
<%= form_for @facet, remote: remote do |f| %>

	<fieldset>
		<%= form_field f.label(:name), f.text_field(:name, :placeholder => "Provide Name") %>
	</fieldset>

	<%= f.submit %>

<% end %>
