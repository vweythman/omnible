<% provide(:title, @tags.index_heading) %>

<header class="index-header">
	<h1><%= @tags.index_heading %></h1>
	<%= @tags.inline_creation %>
	<%= @tags.creation_path %>
	<%= @tags.found_status %>
</header>

<div class="index-body">
	<%= @tags.simple_results %>
</div>

<%= render 'taggings/shared/footer' %>
