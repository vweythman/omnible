<% remote ||= false %>
<%= form_for @tag, remote: remote do |f| %>

	<fieldset class="primary">
		<%= form_field f.label(:name), f.text_field(:name, :placeholder => "Add Tag") %>
	</fieldset>
	<%= submit_panel f %>

<% end %>
