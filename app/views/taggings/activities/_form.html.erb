<% remote ||= false %>
<%= form_for @activity, remote: remote do |f| %>

	<fieldset class="primary">
		<%= string_panel f, :name, "Add Name of Activity" %>
	</fieldset>
	<%= submit_panel f %>

<% end %>
