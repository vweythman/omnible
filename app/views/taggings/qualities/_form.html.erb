<% remote ||= false %>
<%= form_for @quality, remote: remote do |f| %>

	<fieldset class="primary">
		<%= string_panel f, :name, "Add Name" %>
		<%= radio_set(f, :adjective_id, Adjective.all, "Adjective", @quality.adjective_id) %>
	</fieldset>

	<%= submit_panel f %>

<% end %>
