<% provide(:title, 'Tags') %>
<main class='index'>
	<h1>Tags</h1>

	<h2>Activities</h2>
	<%= index_toolkit('Activity', new_activity_path) %>
	<%= render 'descriptors/activities/list' %>

	<h2>Concepts</h2>
	<%= index_toolkit('Concept', new_concept_path) %>
	<%= render 'descriptors/concepts/list' %>

	<h2>Identities</h2>
	<%= index_toolkit('Identity', new_identity_path) %>
	<%= render 'descriptors/identities/list' %>

	<h2>Relationship Types</h2>
	<%= index_toolkit('Relationship Type', new_relator_path) %>
	<%= render 'descriptors/relators/list' %>
</main>

