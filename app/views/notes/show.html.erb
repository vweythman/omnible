<main id="item">
	<%= render 'shared/work_header' %>

	<article class="piece">
		<header>
			<%= model_toolkit(@note) %>
			<h2><%= @note.heading %></h2>
		</header>
		<%= markdown @note.content %>
	</article>

	<%= render 'shared/work_footer' %>
</main>