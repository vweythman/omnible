<%= solo_cell "field" do %>
	<div class="field-content"><%= f.label(value) %></div>
	<div class="field-content">
		<%= f.collection_select(value, group, :id, :heading, {}, :class => "selectables") %>
	</div>
<% end %>
