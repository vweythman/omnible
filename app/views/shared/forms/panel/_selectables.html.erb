<div class="grid-panel">
	<div class="panel-cell solo field">
		<div class="field-content"><%= f.label(value) %></div>
		<div class="field-content"><%= f.collection_select(value, group, :id, :heading, {}, :class => "selectables") %></div>
	</div>
</div>
