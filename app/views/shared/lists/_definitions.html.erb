<dl class="indexable">
	<% listable.each do |key, values| %>
		<dt><%= key %></dt>
		<% values.each do |definition| %>
			<dd><%= link_to definition.name, definition %></dd>
		<% end %>
	<% end %>
</dl>
