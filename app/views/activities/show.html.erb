<% provide(:title, @activity.name) %>

<main id='item'>
  <header>
    <%= model_toolkit @activity %>
    <h1><%= @activity.name %></h1>
    <p>created : <%= record_time @activity.created_at %></p>
  </header>

  <article>
    <h2>Statistics</h2>
  </article>

  <%= render 'shared/tag_footer' %>
<main>
