<%= index_header("Items", new_item_path) %>

<dl class='index'>
	<% @items.each do |key, list| %>
		<dt><%= key %></dt>
		<% list.each do |item| %>
			<dd><%= link_to item.name, item %></dd>
		<% end %>
	<% end %>
</dl>