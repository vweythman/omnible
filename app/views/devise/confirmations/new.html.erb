<h2>Resend confirmation instructions</h2>
<%= clean_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>

	<%= devise_error_messages! %>
	<%= f.email_cell %>
	<%= f.submit_cell "Resend confirmation instructions" %>
<% end %>

<footer>
	<%= render "devise/shared/links" %>
</footer>
