<%= clean_form_for [@story, @chapter] do |f| %>

	<%= error_block(@chapter) %>
	
	<%= f.hidden_field :story_id,  :value => @story.id %>
	<%= render 'form_fields', f: f %>
	<%= f.submit_cell %>

<% end %>
