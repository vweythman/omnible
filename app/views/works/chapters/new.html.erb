<% provide(:title, @story.title + " - New Chapter") %>

<%= creation_block(@story.title + " - #{Chapter.model_name.human}") do %>
	<%= render 'form' %>
<% end %>

<script>
	autosize(document.querySelectorAll('textarea'));
</script>
