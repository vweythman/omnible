<%= clean_form_for(@branch) do |f| %>

	<%= error_block(@branch) %>

	<fieldset class="primary-fieldset">
		<%= render 'works/branches/form_fields', f: f %>
	</fieldset>

	<%= f.submit_cell %>
<% end %>
