<%= form_for(@article) do |f| %>

	<fieldset class="primary">
		<%= render @article.primary_fields, f: f %>
		<%= text_panel f, :article_content, :major, "Write Article" %>
	</fieldset>
	
	<%= render @article.tag_fields, f: f %>

	<%= submit_panel f %>
<% end %>
