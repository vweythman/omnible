<%= form_for [@work, @note] do |f| %>

	<%= f.hidden_field :work_id, :value => @work.id %>

	<fieldset>
		<%= string_panel f, :title, "Write Title (Optional)" %>
		<%= text_panel   f, :content, :major, "Write Content" %>
	</fieldset>

	<%= submit_panel f %>

<% end %>
