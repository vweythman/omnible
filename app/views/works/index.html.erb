<% provide :title, @works.title %>

<header class="index-header">
	<h1><%= @works.heading %></h1>
	<%= @works.for_categories %>
	<%= @works.found_status %>
	<%= @works.creation_path %>
</header>

<div class="index-body">
	<%= @works.results %>
</div>
