<% provide :title, @works.title %>

<div id="main" class="index">
	<header>
		<h1><%= @works.heading %></h1>
		<%= @works.creation_path %>
		<p class="count"><%= @works.total_count %> Found</p>
	</header>

	<%= render 'works/filter' %>
	<%= render 'works/results' %>
</div>
