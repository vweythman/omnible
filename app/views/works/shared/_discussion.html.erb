<div class="comments">
	<%= solo_cell do %>
		<div class="comments-form">
			<%= render 'comments/form', comment: @comment %>
		</div>
	<% end %>

	<%= solo_cell "comments" do %>
		<% if @comments.length > 0 %>
			<h3>Comments</h3>

			<% @comments.each do |comment| %>
			<% end %>
		<% end %>
	<% end %>
</div>
