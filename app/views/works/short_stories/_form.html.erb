<%= form_for(@short) do |f| %>

	<fieldset class="primary">
		<%= render @short.primary_fields, f: f %>

		<%= f.fields_for :chapter do |b| %>
			<%= text_panel b, :content, :major, "Write Story" %>
		<% end %>
	</fieldset>
	
	<%= render @short.tag_fields, f: f %>

	<%= submit_panel f %>
<% end %>
