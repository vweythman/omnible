<%= form_for(@short) do |f| %>

	<fieldset class="primary">
		<%= render @short.primary_fields, f: f %>
		<%= text_panel f, :story_content, :major, "Write Story" %>
	</fieldset>
	
	<%= render @short.tag_fields, f: f %>

	<%= submit_panel f %>
<% end %>
