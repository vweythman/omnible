<% provide(:title, "Anthologies") %>

<div id="main" class="index">
	<header>
		<h1>Anthologies</h1>
		<%= creation_toolkit("Anthology", new_anthology_path) %>
	</header>

	<%= render 'list' %>
</div>
