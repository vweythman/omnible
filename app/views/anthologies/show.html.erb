<% provide(:title, @anthology.main_title) %>

<main id='item'>
	<header>
		<%= model_toolkit @anthology %>
		<h1><%= @anthology.main_title %></h1>
	</header>

	<%= render 'works/list' %>
	<%= render 'footer' %>
<main>
