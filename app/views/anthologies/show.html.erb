<% provide(:title, @anthology.heading) %>

<div id="main" class="feature">
	<header>
		<%= model_toolkit @anthology %>
		<h1><%= @anthology.heading %></h1>
		<div class="summary"><%= markdown @anthology.summary || "" %></div>
	</header>

	<%= render 'works/list' %>
	<%= render 'footer' %>
</div>
