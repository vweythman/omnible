<%= form_for(@anthology) do |f| %>

	<fieldset>
		<%= string_panel f, :name, "Add Name of Anthology" %>
		<%= text_panel   f, :summary, :minor, "Add Summary" %>
	</fieldset>

	<%= render :layout => nested_partial, :locals => { :models => @works } do %>
		<%= @works.fields(f) %>
		<p class="add-cell"><%= link_to_add_association "+ Add Work", f, @works.klass, :partial => @works.partial %></p>
	<% end %>

	<%= submit_panel f %>
<% end %>
