$('<%= @pen.form_finder %>').html("<%= j (render 'form') %>");
$('<%= @pen.form_finder %>').slideDown(180);
$('<%= @pen.tag_finder  %> .pen-title').hide();
$('<%= @pen.kit_finder  %>').hide();
$('<%= @pen.tag_finder  %> th').attr('colspan', 2);
