$('<%= @namer.form_finder %>').html("<%= j (render 'form') %>");
$('<%= @namer.form_finder %>').slideDown(180);
$('<%= @namer.tag_finder  %> .pen-title').hide();
$('<%= @namer.kit_finder  %>').hide();
$('<%= @namer.tag_finder  %> th').attr('colspan', 2);
