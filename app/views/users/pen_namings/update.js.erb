$('<%= @namer.form_finder %>').slideUp(180);
$('<%= @namer.tag_finder  %> .pen-title').show();
$('<%= @namer.tag_finder  %> .pen-title a').html("<%= @namer.name %>");
$('<%= @namer.tag_finder  %> th').attr('colspan', 1);
$('<%= @namer.kit_finder  %>').show();
