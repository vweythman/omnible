<% provide :title, @namer.page_title %>

<header id="dash-header">
	<h1 id="pen-name-<%= @namer.id %>">
		My Dashboard
		<%= label_span "Pen Names" %>
		<%= label_span link_to @namer.name, @namer %>
		<%= @namer.edit_form_div %>
	</h1>
	<div id="pen-name-edit-bar-for-<%= @namer.id %>"><%= @namer.edit_bar %></div>
</header>

<article>

	<section class="the-content">
	</section>
</article>
