<%= show_header(@character) %>

<% if @character.about.empty? && @character.viewpoints.empty? %>
	<%= render 'identities/identities', identities: @character.identities %>
<% else %>
	<article>
		<%= render 'content', character: @character %>
	</article>
<% end %>

<%= character_footer %>
