<div class='nested-fields sortable'>
	<fieldset>
		<%= form_field f.label(:facet_id, "Facet"), f.collection_select(:facet_id, Facet.all, :id, :name, {:selected => (f.object.identity.nil? ? "" : f.object.identity.facet.id)}, {:class=>'selector'})
			#) 
		%>
		<%= form_field f.label(:identity_id, "Descriptor"), f.grouped_collection_select(:identity_id, Facet.all, :identities, :name, :id, :name, {}, {:class=>'selection'}) %>
	</fieldset>

	<p><%= link_to_remove_association "- Remove Descriptor", f %></p>
</div>
